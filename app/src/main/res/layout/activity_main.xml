<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/main"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:padding="16dp"
    tools:context=".MainActivity">

    <com.dennnytech.otpview.OtpView
        android:id="@+id/otpView"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="@dimen/spacing_30"
        app:autoSize="false"
        app:squareCount="6"
        app:maxCountPerLine="6"
        app:squareSize="44dp"
        app:cornerRadius="6dp"
        app:highlightColor="@color/otpViewHighlightColor"
        app:borderColor="@color/otpViewColor"
        app:squareColor="@color/otpViewColor"
        app:textStyle="bold"
        app:textSize="28"
        app:onCompleteBorderColor="@color/colorPrimary"
        app:showCursor="false"
        app:isPassword="true"
        app:borderWidth="1dp"
        app:inputType="number"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
          />

</androidx.constraintlayout.widget.ConstraintLayout>